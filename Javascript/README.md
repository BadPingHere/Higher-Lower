# Higher-Lower Javascript Edition

This is not quite simple. You must download NodeJS, and then run `npm i prompt-sync` in your terminal. From there, you should be able to run this with `node game.js`.
